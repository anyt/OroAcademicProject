{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set widgetContent %}
    {{ oro_widget_render({
        'widgetType': 'block',
        'url': path('anyt_issue_owner_items', { userId: entity.id }),
        'title': 'anyt.bugtracker.user_owner_items.label'|trans
    }) }}
{% endset %}
{{ UI.scrollSubblock(null, [widgetContent]) }}
